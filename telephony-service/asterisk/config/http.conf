;==============================================================================
; taxi-v3 telephony — встроенный HTTP-сервер Asterisk (ARI работает поверх него)
;==============================================================================

[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088
; Security: в production bindaddr=<vpn-ip> или firewall на 8088
; (ARI защищён логином/паролем из ari.conf, но наружу его не открываем).
